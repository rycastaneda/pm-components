import styled from 'styled-components';
import { brandColors } from '../../../common/colors';

export const InteractionStyledIdLink = styled.a`
    font-size: 0.8rem;
    font-weight: 400;
    font-color: ${brandColors.$brandprimary};
`;

export const InteractionItemRow = styled.tr`
    cursor: pointer;  
`;